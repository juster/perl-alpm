pkgname=depconflict
pkgver=1.0
pkgrel=1
pkgdesc="Dep Conflict!"
arch=(i686 x86_64)
license=('GPL')
conflicts=('foo')

build() {
  mkdir -p "$pkgdir/usr/foo"
  echo "DEP CONFLICT!" > "$pkgdir/usr/foo/depconflict"
}
